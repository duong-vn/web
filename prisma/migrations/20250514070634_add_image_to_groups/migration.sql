BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[groups] ADD [image] NVARCHAR(255);

-- AlterTable
ALTER TABLE [dbo].[users] ADD [image] NVARCHAR(1000);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
